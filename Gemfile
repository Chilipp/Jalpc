source 'https://rubygems.org'
group :jekyll_plugins do
    gem 'github-pages'
    gem 'jekyll-email-protect'
    gem 'jekyll-scholar'
    gem 'jekyll-git_metadata'
end

group :test do
    gem 'rake'
    gem 'html-proofer'
end
